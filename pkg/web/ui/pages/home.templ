package pages

import "github.com/fogo-sh/narrows/pkg/web/ui"

templ Home() {
	@ui.Layout("Home") {
		<div class="flex h-screen w-screen flex-col">
			<header class="py-8 text-center text-4xl font-bold">
				welcome to narrows
			</header>
		</div>
	}
}
